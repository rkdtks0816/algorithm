/*
	두 학생 중 누가 더 잘했는지 알려줌
	M 번 질문
	학생 X의
		가장 높은 등수 U
		가장 낮은 등수 V
*/
/*
	학생 1부터 시작
	동점일 경우는 없다.
	입력 앞의 학생이 더 잘헀다
*/
#include<iostream>

using namespace std;

// 입력값
int N, M, X; //학생수, 질문수, 타겟학생
// 변수

// 출력값
int U, V;

// 입력
void Init() {
	cin >> N >> M >> X;
	for (int mi = 0; mi < M; mi++) {
		int tmp
	}
}
// 시뮬
// 타겟 학생을 중심으로 큰지 작은지 확인
// 